const fs=require('fs');const vm=require('vm');const app=fs.readFileSync('app.js','utf8'),html=fs.readFileSync('index.html','utf8');new vm.Script(app);for(const id of ['scanCameraBtn','scanGalleryBtn','photoReaderDialog','visionApiKey','analyzePhotoBtn','photoReviewBody','confirmPhotoImportBtn']){if(!html.includes(`id="${id}"`))throw new Error(`Falta ${id}`);}for(const token of ['navigator.onLine','/v1/responses','input_image','parsePhotoLoadJson','sessionStorage','confirmPhotoImport']){if(!app.includes(token))throw new Error(`Falta lógica ${token}`);}console.log('PASS v5.30: lectura por foto, revisión y modo sin conexión.');